# Acceptance Criteria

All seven canonical routes and legacy redirects load; global navigation/footer are consistent; forms retain the Formspree endpoint and usable labels; query parameters progressively select recognized project types; the CMM project remains clearly identified without unsupported imagery; copy identifies the fixture as a design project; internal links/assets resolve; sitemap contains only canonical routes; responsive navigation works; focus and reduced-motion behavior are present; `CNAME` remains unchanged; no fake claims or imagery are added.
