document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('.review-form');if(!form)return;const status=form.querySelector('.form-status');form.addEventListener('submit',e=>{let ok=true;form.querySelectorAll('[required]').forEach(el=>{if(!el.value.trim()){ok=false;el.classList.add('error');}else{el.classList.remove('error');}});const email=form.querySelector('[type="email"]');if(email&&email.value&&!email.checkValidity()){ok=false;email.classList.add('error');}if(!ok){e.preventDefault();status.textContent='Please complete the required fields before submitting.';return;}const btn=form.querySelector('button[type="submit"]');btn.disabled=true;btn.textContent='Submitting...';});});
